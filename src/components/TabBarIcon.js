import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';

export default TabBarIcon = ({ name, color }) => (
    <Icon
        name={name}
        color={color}
        size={30}
    />
);